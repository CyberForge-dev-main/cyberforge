# 🧠 **CYBERFORGE PROJECT MANUAL**
## For AI Assistants — Deep Verification Edition

**ВЕРСИЯ 2.0 | Актуально: 2025-11-23**  
**Статус:** Требует глубокой верификации перед использованием

> Это НЕ техническая документация.  
> Это инструкция КАК ПРОВЕРЯТЬ и ДУМАТЬ при работе с проектом.

---

## 📖 РАЗДЕЛ 0: КАК ЧИТАТЬ ЭТУ ДОКУМЕНТ

### Что работает (100% проверено)
- ✅ SSH челленджи (dev проверил, они поднимаются)
- ✅ Docker запуск (dev тестировал)
- ✅ Структура проекта (dev показал)

### Что НЕ проверено (не трогай без разборки)
- ❌ Лидерборд (dev не видел работающим)
- ❌ Аутентификация JWT (dev не пробовал)
- ❌ База данных (dev не проверял, не может проверить)
- ❌ Frontend (не известно, работает ли)
- ❌ Флаги в БД (может быть задублировано или неправильно)

### Как использовать этот документ

**ДО начала работы:**
1. Прочитай этот раздел (сейчас читаешь)
2. Перепроверь свои предположения (раздел 1)
3. Определи что РЕАЛЬНО нужно делать (раздел 2)
4. Попроси dev подтверждения (раздел 3)

**ЕСЛИ что-то неясно:**
1. Не гадай — спроси в явном виде
2. Приведи пример что ты думаешь
3. Попроси dev подтвердить или отрицать

---

## 🤔 РАЗДЕЛ 1: ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ ПЕРЕД РАБОТОЙ

### Чек-лист верификации (применяй ВСЕГДА)

```
□ Я прочитал ВСЕ 3 файла истории проекта? 
  (Trebuetsia..., Izuchi..., Sozdai...)

□ Я понимаю что это HOMELAB, не production?
  
□ Я знаю что dev проверил только SSH челленджи?

□ Я не предполагаю, что лидерборд/аутентификация работают?

□ Я готов спрашивать dev вместо того чтобы гадать?

□ Я НЕ буду менять БД structure без подтверждения?

□ Я НЕ буду добавлять PostgreSQL без явного приказа?

□ Я буду FIRST всегда запускать проект и проверять что работает?
```

**Если хотя бы один чек НЕ пройден — СТОП. Спроси у dev.**

---

## 📋 РАЗДЕЛ 2: ЧТО ТАКОЕ CYBERFORGE (ПЕРЕОПРЕДЕЛЕНИЕ)

### Факт 1: Это учебная платформа
- **Цель:** люди на одном/разных компьютерах тренируют навыки кибербезопасности
- **Формат:** SSH челленджи + возможно web лабы в будущем
- **Развертывание:** один хост запускает Docker Compose, другие подключаются

### Факт 2: Текущее состояние (Phase 2)
- ✅ **SSH челленджи** развернуты и тестировались
- ❓ **Backend API** — существует код, но НЕ полностью проверен
- ❓ **Frontend** — существует, но неизвестно, подключается ли к API
- ❓ **БД** — SQLite, но содержимое неизвестно
- ❓ **Флаги** — спрятаны в SSH челленджах, но может быть ошибка синхронизации

### Факт 3: Это НЕ MVP
- "MVP" — слово-ловушка, которое нейросети используют как оправдание
- CyberForge задумано как **рабочая платформа**, которая может расти
- Если что-то частично работает, это не значит что это "как для MVP"
- Это значит что нужна ПОЛНАЯ ВЕРИФИКАЦИЯ

### Факт 4: Цель этого проекта
```
Homelab для практики безопасности:
  ↓
Несколько компов в сети подключаются
  ↓
Решают SSH челленджи (и позже web лабы)
  ↓
Отслеживаются результаты
  ↓
(Может быть локальный лидерборд)
  ↓
Повторяется
```

---

## 🔍 РАЗДЕЛ 3: ПРОЦЕСС ВЕРИФИКАЦИИ (ИНСТРУКЦИЯ ДЛЯ НЕЙРОСЕТИ)

### Когда что-то "не работает"

**Шаг 1: Не верь предположениям**
```
❌ "Backend не работает" (откуда ты знаешь?)
✅ "Я запустил `curl http://localhost:5000/api/health` и получил ошибка X"
```

**Шаг 2: Собери ТОЧНЫЙ контекст**
```
Спроси:
- Какой точно файл?
- Какая строка?
- Какой точный вывод ошибки?
- Ты это сам видел или читал в сообщении?
```

**Шаг 3: Разделяй проблемы**
```
❌ "Всё сломано"
✅ "Docker запущен ОК, но backend port 5000 не открывается"
```

**Шаг 4: Проверь цепь зависимостей**
```
SSH челленджи работают? → Да, dev проверил
Backend поднимается? → Неизвестно, нужна проверка
Frontend подключается? → Неизвестно, нужна проверка
БД имеет правильные флаги? → Неизвестно, нужна проверка
```

**Шаг 5: Попроси dev самостоятельно воспроизвести**
```
Вместо того чтобы делать предположения:
"Dev, можешь запустить эту команду и показать вывод?"
```

---

## 🏗️ РАЗДЕЛ 4: СТРУКТУРА (ТОЛЬКО ПРОВЕРЕННЫЕ ФАКТЫ)

### Что существует (dev видел файлы)

```
~/Documents/cyberforge/
├─ backend/          # Flask приложение
│  ├─ app.py        # Существует
│  ├─ config.py     # Существует (SQLite URI там)
│  └─ requirements.txt
│
├─ challenges/       # SSH челленджи
│  ├─ ch1/Dockerfile
│  ├─ ch2/Dockerfile
│  └─ ch3/Dockerfile
│
├─ docker-compose.yml  # Существует
└─ [другое]

```

### Что НЕИЗВЕСТНО

```
- Все ли порты в docker-compose открыты?
- Все ли челленджи имеют правильные флаги?
- БД инициализируется правильно?
- Frontend вообще подключается к backend?
- Лидерборд показывает правильные данные?
```

---

## 🚀 РАЗДЕЛ 5: ПРАВИЛА (АБСОЛЮТНЫЕ)

### ✅ МОЖНО

1. **Запускать docker compose up --build** и смотреть логи
2. **Проверять**: `curl http://localhost:XXXX`
3. **Тестировать SSH**: `ssh -p 2222 user@localhost`
4. **Читать код** и искать противоречия
5. **Спрашивать** точные вопросы

### ❌ НИКОГДА

1. 🚫 Менять БД схему без явного приказа
2. 🚫 Добавлять PostgreSQL (только SQLite)
3. 🚫 Предполагать что лидерборд работает
4. 🚫 Предполагать что аутентификация работает
5. 🚫 Рефакторить код "для красоты"
6. 🚫 Удалять файлы которые "выглядят лишними"
7. 🚫 Менять флаги без проверки что они везде одинаковые

---

## 📊 РАЗДЕЛ 6: ТЕКУЩИЕ ВОПРОСЫ (ДЛЯ DEV)

**Нейросеть НЕ может ответить на эти вопросы. Dev должен.**

### Критические вопросы

```
Q1: Лидерборд работает или нет? (dev не видел)
Q2: Аутентификация работает или нет? (dev не пробовал)
Q3: БД заполняется с правильными флагами? (dev не проверял)
Q4: Frontend подключается к backend? (dev не знает)
Q5: SSH челленджи имеют одинаковые флаги везде? (нужна проверка)
Q6: Какие роли пользователей? (admin, user, guest?)
Q7: Как admin запускает новые челленджи? (есть ли интерфейс?)
```

### Как dev должен отвечать

```
НЕ так:
"Лидерборд примерно работает"

ТАК:
"Я запустил систему, зашёл в браузер на :3000, 
кликнул 'Leaderboard', получился результат X, 
это правильно потому что Y"
```

---

## 🔧 РАЗДЕЛ 7: ДИАГНОСТИКА (DECISION TREE)

### Алгоритм диагностики для нейросети

```
ВОПРОС: Что мне делать?

1️⃣ ЭТО ОПАСНАЯ ОПЕРАЦИЯ?
   (Менять БД, удалять файлы, менять архитектуру)
   → ДА: Спроси dev, не делай
   → НЕТ: Продолжай

2️⃣ ЕСТЬ ЛИ ТОЧНЫЙ КОНТЕКСТ?
   (Какой файл, какая ошибка, какой вывод)
   → ДА: Продолжай
   → НЕТ: Спроси у dev, получи подробности

3️⃣ ЭТО ЗАТРАГИВАЕТ НЕПРОВЕРЕННУЮ ЧАСТЬ?
   (Лидерборд, аутентификация, БД)
   → ДА: Сначала верифицируй с dev, потом действуй
   → НЕТ: Продолжай

4️⃣ ИМЕЕШЬ ЛИ ТЫ ПОЛНЫЙ КОД?
   (Видишь ли ты весь код соответствующего файла?)
   → ДА: Анализируй код, делай выводы
   → НЕТ: Попроси у dev полный файл

5️⃣ ГОТОВ ЛИ TЫ ТЕСТИРОВАТЬ ЛОКАЛЬНО?
   (Можешь ли ты запустить `docker compose` и проверить?)
   → ДА: Делай и отчитывайся о результатах
   → НЕТ: Эскалируй к dev, описывая проблему
```

---

## 📝 РАЗДЕЛ 8: ПРИМЕРЫ ХОРОШЕГО И ПЛОХОГО ПОВЕДЕНИЯ

### ❌ ПЛОХО: Слепо действовать

**Нейросеть:**
```
"Я вижу что в config.py postgresql, значит надо изменить на sqlite"
[меняет конфиг]
[после перезагрузки всё сломалось]
```

**Почему плохо:** Не проверил что реально используется БД.

---

### ✅ ХОРОШО: Спросить и проверить

**Нейросеть:**
```
"Я вижу в config.py строка postgresql URI. 
Это означает что система использует PostgreSQL?
Или это старый код, который не используется?
Dev, можешь подтвердить?"
```

---

### ❌ ПЛОХО: Предполагать что знаешь

**Нейросеть:**
```
"Лидерборд показывает игроков, значит работает правильно"
```

**Почему плохо:** Dev не видел лидерборд, неизвестно правильно ли он работает.

---

### ✅ ХОРОШО: Проверить фактически

**Нейросеть:**
```
"Dev, я вижу что есть код лидерборда в app.py.
Ты это тестировал? Показывает ли правильные результаты?
Если не знаешь, давай я напишу скрипт проверки"
```

---

## 🎯 РАЗДЕЛ 9: ТИПИЧНЫЕ ЛОВУШКИ

### Ловушка 1: "MVP оправдание"

```
❌ НЕПРАВИЛЬНО:
"Это просто MVP, поэтому лидерборд 
может не совсем работать"

✅ ПРАВИЛЬНО:
"Лидерборд либо работает, либо не работает.
Нужна проверка. Если не работает,
это баг который нужно чинить."
```

### Ловушка 2: "Я уверен что я знаю"

```
❌ НЕПРАВИЛЬНО:
"Ясно что backend падает потому что БД 
не инициализируется"

✅ ПРАВИЛЬНО:
"Я вижу ошибку port 5000 не открывается.
Это может быть:
1. Backend упал (проверю логи)
2. Port занят другим процессом (проверю lsof)
3. Docker контейнер не поднялся (проверю docker ps)

Dev, помоги определить какой именно?"
```

### Ловушка 3: "Я просто изменю это"

```
❌ НЕПРАВИЛЬНО:
"config.py использует SQLite, 
это неправильно, буду использовать PostgreSQL"
[изменяет без разрешения]

✅ ПРАВИЛЬНО:
"Я вижу что config.py использует SQLite.
Dev, это намеренное решение или legacy код?
Нужно ли менять?"
```

---

## 🧪 РАЗДЕЛ 10: КОГДА НЕЙРОСЕТЬ МОЖЕТ ДЕЙСТВОВАТЬ САМОСТОЯТЕЛЬНО

### Категория 1: Очевидные баги в коде

**Пример:** Переменная инициализируется дважды, second перезаписывает first.

**Действие:** Нейросеть может чинить (с объяснением).

```
"Я вижу в app.py строка X и строка Y 
где Y перезаписывает X. 
Это выглядит как баг. 
Я это чиню и вот почему [объяснение]"
```

### Категория 2: Добавлять тесты и мониторинг

**Пример:** Написать bash скрипт который проверяет что все порты открыты.

**Действие:** Нейросеть может добавлять (не трогая основной код).

---

## 📞 РАЗДЕЛ 11: ПОМОЩЬ ДЛЯ ДРУГИХ НЕЙРОСЕТЕЙ

### Как читать этот документ в будущих чатах

```
ПЕРВЫЙ ЧАС:
Раздел 0 + Раздел 1 (что я ВООБЩЕ могу делать?)

ПЕРЕД НАЧАЛО РАБОТЫ:
Раздел 3 + Раздел 5 (правила и процесс)

ЕСЛИ НЕПОНЯТНО:
Раздел 9 (типичные ловушки)

ЕСЛИ СЛОМАЛОСЬ:
Раздел 7 (decision tree)
```

### Контрольные вопросы (задай себе)

```
1. Я точно знаю что проверено и что нет?
2. Я спросил dev вместо того чтобы предполагать?
3. Я дал точный контекст (код, вывод, команда)?
4. Я готов тестировать локально?
5. Я не буду менять что-то без подтверждения?

Если ВСЕ ответы "ДА" → начинай работу.
```

---

## 🎓 РАЗДЕЛ 12: МЕТАИНФОРМАЦИЯ

### О промпт-инжиниринге (что использовано в этом документе)

**Техники:**
1. **Verification First** — всегда проверяй
2. **Explicit Uncertainty** — говори что не знаешь
3. **Role Assignment** — dev vs AI чётко разделены
4. **Constraint Specification** — явные ограничения
5. **Decision Trees** — пошаговая логика
6. **Self-Critique** — встроенная проверка себя
7. **Few-Shot Examples** — примеры хорошего/плохого

### Почему этот документ лучше старого

**Старый:**
```
Много технических деталей
Предположения что всё работает
Нейросеть может делать что угодно
Результат: косяки и поломки
```

**Новый:**
```
Только ПРОВЕРЕННЫЕ факты
Явные границы что работает
Нейросеть действует по процессу
Результат: контролируемая разработка
```

---

## 📋 ИСТОРИЯ ВЕРСИЙ

| V | Дата | Что изменилось |
|---|------|----------------|
| 1.0 | 2025-11-23 | Старая версия (много предположений) |
| 2.0 | 2025-11-23 | Переделана на основе prompt engineering. Только факты + инструкции. |

---

**СОЗДАНО:** 2025-11-23  
**ОБНОВЛЕНО:** 2025-11-23  
**КОНТЕКСТ:** CyberForge Homelab Platform  
**ДЛЯ:** AI Assistants во всех будущих чатах

---

> **ГЛАВНОЕ ПРАВИЛО:**  
> **Если не уверен — спроси.  
> Если спросил — ждал ответа.  
> Если ответ пришёл — действуй по процессу.**